pipeline {
    agent any
    stages {
        stage('Setup') {
            steps {
                sh 'apt pip install robot-framework'
                sh 'apt pip install robotframework-databaselibrary'
                sh 'apt pip install pymssql'
            }
        }
        stage('Tests: Robot Framework') {
            steps {
                sh 'robot .'
            }
        }
    }
}
