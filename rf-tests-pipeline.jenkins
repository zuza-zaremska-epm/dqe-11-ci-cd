pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        echo 'Building...'
        echo 'Running ${env.BUILD_ID} $env.BUILD_DISPLAY_NAME} on ${env.NODE_NAME} and JON ${env.JOB_NAME}"
      }
    }
    stage('List') {
      steps {
        sh("dir ${JENKINS_HOME}")
      }
    }
    stage('Tests') {
      steps {
        echo 'Testing...'
      }
    }
    stage('Deploy') {
      steps {
        echo 'Deploying...'
      }
    }
  }
}
