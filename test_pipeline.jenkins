pipeline {
    agent any
    stages {
        stage('Version') {
            steps {
                sh 'python3 --version'
                sh 'pip --version'
            }
        }
        stage('Tests') {
            steps {
                sh 'pytest test_homework.py'
            }
        }
    }
}
